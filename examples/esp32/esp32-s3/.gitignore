# ESP-IDF build output + generated config (the WiFi build has its own, see README "WiFi")
/build/
/build-wifi/
sdkconfig
sdkconfig.old
sdkconfig.ci
sdkconfig.wifi
sdkconfig.wifi.old

# Build/flash logs (from idf.py wrapper runs)
*.log

# ESP-IDF Component Manager artifacts (created if managed dependencies are added later)
dependencies.lock
/managed_components/

# IDE / stray CMake configure dirs (FetchContent caches its downloads under these)
/cmake-build-*/
/.idea/
