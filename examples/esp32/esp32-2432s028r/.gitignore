# ESP-IDF build output + generated config
/build/
sdkconfig
sdkconfig.old
sdkconfig.ci

# The AOT app (app.gen.c/.h) is generated by `npm run aot` into bridges/quickjs/js/dist and compiled
# from there — nothing app-derived lands in this project tree.

# Build/flash logs
*.log

# ESP-IDF Component Manager artifacts
dependencies.lock
/managed_components/

# IDE / stray CMake configure dirs (FetchContent caches its downloads under these)
/cmake-build-*/
/.idea/
