# examples/linux

Desktop sample app. Engine + `backends/sdl/` + `bridges/quickjs/` running a small React
JSX component inside an SDL2 window.

This is the first example planned to be real end-to-end — it gates the Flow A toolchain
(see `/PLAN.md`).

**Status:** Planned. No code yet.
