{
  "//": "Typecheck the shipped public type declarations (src/embedded-react/index.d.ts) so they don't rot, plus the usage fixtures in test/types that exercise them the way an app does \u2014 compiling the declarations alone cannot catch a signature that is well-formed but wrong. Run with `npm run typecheck`. The runtime is JavaScript; this checks types only.",
  "compilerOptions": {
    "target": "ES2020",
    "lib": [
      "ES2020"
    ],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true
  },
  "files": [
    "src/embedded-react/index.d.ts",
    "test/types/gesture-usage.tsx"
  ]
}
