{
  "name": "embedded-react-quickjs-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "React reconciler + host config that drives the embedded-react engine through the QuickJS NativeUI bridge (Flow A).",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-reconciler": "0.29.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
