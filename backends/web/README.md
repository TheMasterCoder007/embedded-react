# backends/web

WebAssembly backend — the engine is compiled to WASM and rendered into a
`<canvas>` via either Canvas 2D or WebGL. Lets a React app authored against
`embedded-react` run unmodified in a browser preview.

**Status:** Planned. No code yet.
