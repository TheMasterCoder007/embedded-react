# backends/framebuffer

Generic Linux `/dev/fb0` backend — for SBCs and embedded Linux boards without a
compositor. CPU blit writes directly into the kernel framebuffer.

**Status:** Planned. No code yet.
