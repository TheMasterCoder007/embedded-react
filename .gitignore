# JetBrains
.idea/

# CMake build directories
build/
build-*/
cmake-build-*/
out/

# Compiled output
*.o
*.a
*.elf
*.bin
*.hex
*.map
*.d

# Font converter output
tools/font-converter/build/
*.ttf.h

# Test artifacts
tests/*/build/
Testing/

# Node deps / tool caches (real deps live in bridges/quickjs/js; this catches stray caches at any level)
node_modules/
.vite/

# WASM simulator build output — the .wasm/.js are built (locally or by CI) and shipped prebuilt in the
# npm package, never committed (mirrors how dist/ is handled). See WASM_SIM.md.
tools/web-sim/public/
# Prebuilt sim assets staged into the npm package by build.mjs (shipped via the package's files whitelist).
bridges/quickjs/js/sim/

# OS
.DS_Store
Thumbs.db
